from setuptools import setup, find_packages

setup(name="betadisk",
        version = "0.1",
        author = "Johan Olofsson",
        author_email = "olofsson@mpia.de",
        url = "https://github.com/joolof/betadisk",
        packages = find_packages(),
        )
